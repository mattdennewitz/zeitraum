---
phase: 05-gui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/ZeitraumLookAndFeel.h
  - src/ui/TopBar.h
  - src/PluginEditor.h
  - src/PluginEditor.cpp
  - src/PluginProcessor.cpp
autonomous: true
requirements:
  - GUI-01

must_haves:
  truths:
    - "Plugin opens a 900x500 resizable window with dark background and teal accent theme"
    - "Top bar shows linear sliders for base delay, multiplier, mix, character and toggles for quantize, tempo sync, plus dropdowns for note division and output mix"
    - "All top-bar controls are wired to APVTS parameters and update when automated"
    - "Window is resizable with minimum ~700x400, layout adapts proportionally"
  artifacts:
    - path: "src/ui/ZeitraumLookAndFeel.h"
      provides: "Dark/teal LookAndFeel_V4 subclass with ColourScheme"
    - path: "src/ui/TopBar.h"
      provides: "Horizontal strip of global controls with APVTS attachments"
    - path: "src/PluginEditor.h"
      provides: "Custom editor with layout skeleton (top bar, left panel placeholder, right panel placeholder)"
    - path: "src/PluginEditor.cpp"
      provides: "Resizable editor with LookAndFeel, layout in resized()"
  key_links:
    - from: "src/PluginEditor.cpp"
      to: "src/PluginProcessor.h"
      via: "processorRef.apvts for parameter attachments"
      pattern: "processorRef\\.apvts"
    - from: "src/PluginProcessor.cpp"
      to: "src/PluginEditor.h"
      via: "createEditor returns ZeitraumEditor"
      pattern: "new ZeitraumEditor"
---

<objective>
Build the plugin's visual foundation: custom LookAndFeel theme, resizable editor shell with proportional layout, and top bar with all global controls wired to APVTS parameters.

Purpose: Establishes the dark/teal theme and layout skeleton that all subsequent GUI components slot into. Replaces the GenericAudioProcessorEditor with a custom editor.
Output: Working plugin window at 900x500 with themed top bar controls, placeholder areas for tap columns (left) and feedback matrix (right).
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gui/05-CONTEXT.md
@.planning/phases/05-gui/05-RESEARCH.md
@src/PluginProcessor.h
@src/PluginEditor.h
@src/PluginEditor.cpp
@src/PluginProcessor.cpp

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/PluginProcessor.h:
```cpp
class ZeitraumProcessor : public juce::AudioProcessor
{
public:
    // Tap preset management
    void saveTapPreset(const juce::String& name);
    void recallTapPreset(const juce::String& name);
    juce::StringArray getTapPresetNames() const;

    juce::AudioProcessorValueTreeState apvts;
};
```

Parameter IDs (from PluginProcessor.cpp createParameterLayout):
- Global: BASE_DELAY, MULTIPLIER, MIX, CHARACTER, QUANTIZE, TEMPO_SYNC, NOTE_DIV, OUTPUT_MIX
- Per-tap: TAP1_POS..TAP8_POS, TAP1_LEVEL..TAP8_LEVEL
- Feedback: FB_TAP1..FB_TAP8, FB_ODD, FB_EVEN, FB_RISING, FB_FALLING
- Filters: FB_HP_FREQ, FB_LP_FREQ, FB_HP_ON, FB_LP_ON

Current createEditor (line 331-334 of PluginProcessor.cpp):
```cpp
juce::AudioProcessorEditor* ZeitraumProcessor::createEditor()
{
    return new juce::GenericAudioProcessorEditor(*this);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LookAndFeel theme and TopBar component</name>
  <files>src/ui/ZeitraumLookAndFeel.h, src/ui/TopBar.h</files>
  <action>
Create two header-only files in src/ui/:

**ZeitraumLookAndFeel.h:**
- Subclass juce::LookAndFeel_V4
- Constructor initializes ColourScheme with dark/teal palette:
  - windowBackground: 0xff2d2d2d
  - widgetBackground: 0xff3a3a3a
  - menuBackground: 0xff252525
  - outline: 0xff555555
  - defaultText: 0xffcccccc
  - defaultFill: 0xff00bcd4 (teal accent)
  - highlightedText: 0xffffffff
  - highlightedFill: 0xff00acc1
  - menuText: 0xffcccccc
- Override drawLinearSlider to render a clean linear bar with teal fill, value text always visible
- Override drawToggleButton for flat toggle style with teal active state
- Override drawComboBox for dark dropdown style
- Override drawPopupMenuItem for dark popup menu style
- Use `juce::` prefix everywhere (DONT_SET_USING_JUCE_NAMESPACE=1)

**TopBar.h:**
- juce::Component subclass
- Constructor takes juce::AudioProcessorValueTreeState& reference
- Contains member components:
  - juce::Slider for BASE_DELAY (LinearBar, horizontal), MULTIPLIER, MIX, CHARACTER
  - juce::ToggleButton for QUANTIZE, TEMPO_SYNC
  - juce::ComboBox for NOTE_DIV (populate items: "1/4", "1/8", "1/4 dotted", "1/8 dotted", "1/4 triplet", "1/8 triplet" BEFORE creating attachment), OUTPUT_MIX (populate: "Manual", "Odd", "Even", "Rising", "Falling")
- Corresponding attachment members:
  - std::unique_ptr&lt;juce::AudioProcessorValueTreeState::SliderAttachment&gt; for each slider
  - std::unique_ptr&lt;juce::AudioProcessorValueTreeState::ButtonAttachment&gt; for each toggle
  - std::unique_ptr&lt;juce::AudioProcessorValueTreeState::ComboBoxAttachment&gt; for each combo
- CRITICAL: Populate ComboBox items BEFORE creating ComboBoxParameterAttachment (see Pitfall 2 in research)
- Each slider has a juce::Label to its left showing the parameter name
- Sliders should show value + unit suffix in their text (ms for delay, x for multiplier, % for mix/character)
- resized() uses juce::FlexBox with Direction::row to lay out controls horizontally with reasonable flex proportions
- addAndMakeVisible all children in constructor
  </action>
  <verify>
    <automated>cd /Users/matt/src/multi-tap-delay && make 2>&1 | tail -5</automated>
  </verify>
  <done>ZeitraumLookAndFeel.h compiles with dark/teal ColourScheme and custom draw overrides. TopBar.h compiles with all global controls and APVTS attachments.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild PluginEditor with layout skeleton and switch createEditor</name>
  <files>src/PluginEditor.h, src/PluginEditor.cpp, src/PluginProcessor.cpp</files>
  <action>
**PluginEditor.h — complete rewrite:**
- Include "ui/ZeitraumLookAndFeel.h" and "ui/TopBar.h"
- ZeitraumEditor : public juce::AudioProcessorEditor
- Members declared in THIS ORDER (LookAndFeel first for correct destruction order — see Pitfall 1):
  1. ZeitraumLookAndFeel lookAndFeel;
  2. ZeitraumProcessor& processorRef;
  3. TopBar topBar;
- Constructor, destructor, paint(), resized()

**PluginEditor.cpp — complete rewrite:**
- Constructor:
  - Call setLookAndFeel(&lookAndFeel)
  - Initialize topBar with processorRef.apvts
  - addAndMakeVisible(topBar)
  - setResizable(true, true)
  - setResizeLimits(700, 400, 1400, 900)
  - setSize(900, 500)
- Destructor: call setLookAndFeel(nullptr)
- paint(): fillAll with windowBackground colour from LookAndFeel, draw a thin line below top bar area as separator. In the left placeholder area (~60% of main content) draw dim text "Tap Columns" and in the right placeholder (~40%) draw "Feedback Matrix" — these placeholders help verify layout before Plan 02/03 fill them.
- resized():
  - Top bar: fixed height ~50px from top
  - Main area: bounds.reduced(8, 4) below top bar
  - Left panel: 60% of main width (for future tap columns)
  - Right panel: 40% of main width (for future feedback matrix + filter controls)

**PluginProcessor.cpp — single change:**
- Change createEditor() to return `new ZeitraumEditor(*this)` instead of GenericAudioProcessorEditor
- Add `#include "PluginEditor.h"` if not already present (check — it may already be included)

After all changes: `make` must compile and `make test` must pass (existing tests should not break since no DSP or parameter changes).
  </action>
  <verify>
    <automated>cd /Users/matt/src/multi-tap-delay && make 2>&1 | tail -5 && make test 2>&1 | tail -10</automated>
  </verify>
  <done>Plugin compiles with custom editor at 900x500. Opening plugin shows dark themed window with top bar controls. Resizing works between 700x400 and 1400x900. All existing tests pass. createEditor returns custom ZeitraumEditor.</done>
</task>

</tasks>

<verification>
1. `make` compiles without errors
2. `make test` — all existing tests pass (no DSP/parameter changes)
3. Plugin window opens at 900x500 with dark background
4. Top bar shows all 8 global controls (4 sliders, 2 toggles, 2 dropdowns) with teal accent
5. Window resizes between 700x400 and 1400x900 with proportional layout
6. Controls are wired to parameters (moving a slider changes the APVTS value)
</verification>

<success_criteria>
- Custom LookAndFeel with dark/teal theme applied to all components
- Top bar with all global controls functional and APVTS-attached
- Resizable editor with proportional layout skeleton
- GenericAudioProcessorEditor fully replaced
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-gui/05-01-SUMMARY.md`
</output>
