---
phase: 01-project-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - INFR-03

must_haves:
  truths:
    - "Running `make validate` passes AU validation via auval without errors"
    - "The plugin appears in auval's AU registry with correct codes and channel configuration"
  artifacts: []
  key_links:
    - from: "Makefile validate target"
      to: "auval -v aufx ZtRm DsEr"
      via: "killall AudioComponentRegistrar + auval invocation"
      pattern: "auval.*aufx.*ZtRm.*DsEr"
---

<objective>
Validate the AU plugin passes auval and confirm the complete scaffold is working.

Purpose: INFR-03 requires AU validation. auval is strict about bus layouts, tail length, and entry points -- this confirms everything is wired correctly.
Output: Confirmed AU validation pass.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffolding/01-CONTEXT.md
@.planning/phases/01-project-scaffolding/01-RESEARCH.md
@.planning/phases/01-project-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run AU validation and fix any issues</name>
  <files></files>
  <action>
1. First, confirm the plugin is registered by running: `auval -a | grep -i zeitraum`
   - This reveals the exact AU type, subtype code, and manufacturer code as registered
   - If the codes don't match what the Makefile expects (ZtRm / DsEr), update the Makefile PLUGIN_AU_CODE and PLUGIN_MFR_CODE to match

2. Run `make validate` which executes:
   - `killall -9 AudioComponentRegistrar` (clears AU cache)
   - `sleep 1`
   - `auval -v aufx ZtRm DsEr`

3. If auval fails:
   - "No matching AU found": Check codes with `auval -a | grep -i zeitraum`, update Makefile codes to match
   - Bus layout failures: Verify isBusesLayoutSupported only accepts stereo/stereo
   - Tail length issues: Verify getTailLengthSeconds returns 0.0
   - Output garbage: Verify processBlock clears extra output channels
   - Fix the issue in source code, rebuild with `make`, and re-run `make validate`

4. Capture the auval output showing "PASS" for inclusion in the summary.
  </action>
  <verify>
    <automated>cd /Users/matt/src/multi-tap-delay && make validate 2>&1 | tail -5</automated>
  </verify>
  <done>auval reports validation passed for aufx ZtRm DsEr (or corrected codes if auval registered different codes)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify plugin loads in DAW</name>
  <files></files>
  <action>
User verifies the plugin loads and passes audio in their DAW. This cannot be automated -- requires a real DAW host.
  </action>
  <verify>User confirmation</verify>
  <done>User confirms plugin appears in DAW plugin list and passes audio through unchanged</done>
  <what-built>Complete Zeitraum plugin scaffold: compiling VST3+AU pass-through plugin with Makefile workflow, Catch2 tests, and CLAUDE.md</what-built>
  <how-to-verify>
    1. Open your DAW (Logic, Ableton, REAPER, etc.)
    2. Add an effect plugin -- look for "Zeitraum" by "Die stille Erde" in the plugin list
    3. Insert it on a track with audio
    4. Confirm audio passes through unchanged (pass-through behavior)
    5. The plugin window should show a generic parameter editor (empty, since no parameters yet)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `make validate` completes with auval reporting PASS
2. Plugin visible in DAW plugin list as "Zeitraum"
3. Audio passes through the plugin unchanged
</verification>

<success_criteria>
- AU validation passes via auval (INFR-03)
- Plugin loads in a real DAW and passes audio through
- Complete phase 1 scaffold is verified end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding/01-02-SUMMARY.md`
</output>
