---
phase: 04-daw-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/PluginProcessor.cpp
  - test/PluginTests.cpp
autonomous: false
requirements:
  - INTG-03

must_haves:
  truths:
    - "Saving and reopening a DAW session restores all plugin state exactly including tap presets and feedback matrix"
    - "Loading a session saved with plugin version 2 (pre-tempo sync) works correctly with silent defaults"
    - "Plugin passes AU validation after all Phase 4 changes"
  artifacts:
    - path: "src/PluginProcessor.cpp"
      provides: "pluginVersion 3 in getStateInformation"
      contains: "pluginVersion.*3"
    - path: "test/PluginTests.cpp"
      provides: "Backward compatibility and state persistence tests"
      contains: "backward"
  key_links:
    - from: "src/PluginProcessor.cpp (getStateInformation)"
      to: "src/PluginProcessor.cpp (setStateInformation)"
      via: "XML with pluginVersion attribute"
      pattern: "pluginVersion"
---

<objective>
Bump state persistence version to 3, add backward compatibility tests for older sessions, and validate the complete Phase 4 build via AU validation.

Purpose: Ensures session recall works perfectly including cross-version compatibility, and the plugin passes Apple's AU validation after all changes.
Output: Version-bumped state persistence, comprehensive state tests, and verified AU validation.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-daw-integration/04-CONTEXT.md
@.planning/phases/04-daw-integration/04-01-SUMMARY.md
@src/PluginProcessor.h
@src/PluginProcessor.cpp
@test/PluginTests.cpp

<interfaces>
From src/PluginProcessor.cpp (after Plan 01):
```cpp
// getStateInformation currently sets pluginVersion = 2
xml->setAttribute("pluginVersion", 2);

// setStateInformation reads version but doesn't use it
int version = xml->getIntAttribute("pluginVersion", 1);
```

Expected new parameters from Plan 01:
- TEMPO_SYNC (bool, default false)
- NOTE_DIV (choice, default index 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump pluginVersion to 3 and add backward compatibility state tests</name>
  <files>src/PluginProcessor.cpp, test/PluginTests.cpp</files>
  <behavior>
  - Test: getStateInformation produces XML with pluginVersion=3
  - Test: State saved with version 2 XML (no TEMPO_SYNC or NOTE_DIV) loads correctly -- missing params get defaults (false, 1)
  - Test: State saved with modified feedback gains, tap positions, and tempo sync all round-trip correctly
  - Test: Null data and zero-size data to setStateInformation are handled gracefully (no crash)
  - Test: Wrong XML tag name is rejected gracefully
  </behavior>
  <action>
1. In src/PluginProcessor.cpp getStateInformation(), change:
   ```cpp
   xml->setAttribute("pluginVersion", 3);  // was 2
   ```

2. Add tests to test/PluginTests.cpp:

   "State version is 3" -- save state, parse XML, verify pluginVersion attribute is 3.

   "Backward compatible with version 2 state" -- manually construct a version 2 XML state (using the known APVTS tag name "PARAMETERS") with some params set (e.g., BASE_DELAY=100) but WITHOUT TEMPO_SYNC or NOTE_DIV attributes. Load into a new processor. Verify BASE_DELAY restored to 100, TEMPO_SYNC defaults to false (0), NOTE_DIV defaults to 1.

   "Full state round-trip with all Phase 4 params" -- create processor, set BASE_DELAY=120, TEMPO_SYNC=true, NOTE_DIV=3 (triplet 1/8), FB_TAP1=50, TAP3_POS=0.5. Save state. Create new processor, restore. Verify all values match.

   "setStateInformation handles null data" -- call with nullptr and 0 size, verify no crash.

   "setStateInformation rejects wrong XML tag" -- create XML with wrong tag name, verify state is not replaced (existing test may cover this, extend if needed).
  </action>
  <verify>
    <automated>cd /Users/matt/src/multi-tap-delay && make test</automated>
  </verify>
  <done>
  - pluginVersion bumped to 3 in getStateInformation
  - Backward compat test proves version 2 state loads with correct defaults for new params
  - Full round-trip test proves all Phase 4 params persist correctly
  - Edge case tests prove robustness against corrupt/null state data
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: AU validation and DAW verification of Phase 4 integration</name>
  <files>none</files>
  <action>
Run AU validation via `make validate`. Then load the plugin in a DAW for manual verification of parameter grouping, tempo sync, and state persistence.
  </action>
  <what-built>Complete Phase 4 DAW integration: parameter grouping, tempo sync, state persistence v3</what-built>
  <how-to-verify>
    1. Run AU validation:
       ```
       cd /Users/matt/src/multi-tap-delay && make validate
       ```
       Expected: auval passes for aufx ZtRm DsEr

    2. Load plugin in DAW (Logic, Reaper, or similar):
       - Verify parameters appear in organized groups in automation lanes (Global, Tap 1-8, Feedback, Output)
       - Verify parameter values show units (ms, %, Hz, x)
       - Enable Tempo Sync -- verify delay time changes to match host BPM
       - Change Note Division -- verify delay time updates
       - Change host BPM -- verify delay time smoothly follows
       - Save session, close, reopen -- verify all settings restored

    3. Optional: Load a session saved before Phase 4 changes -- verify it loads without issues (missing tempo sync params use defaults)
  </how-to-verify>
  <verify>
    <automated>cd /Users/matt/src/multi-tap-delay && make validate</automated>
  </verify>
  <done>
  - AU validation passes
  - User confirms organized parameter groups in DAW automation lanes
  - User confirms tempo sync works with host BPM
  - User confirms session save/restore works
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `make test` passes all tests (existing + new state tests)
- `make validate` passes AU validation
- DAW session save/restore works with all Phase 4 parameters
</verification>

<success_criteria>
- pluginVersion is 3 in saved state XML
- Backward compatibility proven: version 2 state loads with correct defaults
- Full state round-trip works for all parameters including TEMPO_SYNC and NOTE_DIV
- AU validation passes
- User confirms DAW automation lanes show organized parameter groups
- User confirms tempo sync works in DAW with host BPM
</success_criteria>

<output>
After completion, create `.planning/phases/04-daw-integration/04-02-SUMMARY.md`
</output>
